# UI Design Baseline (2026-05-21)

This folder is now the **tracked source of truth** for the 21st.dev-inspired UI baseline.

Included:
- `DESIGN_SYSTEM.md` — full design laws/tokens/motion rules
- `design-preview.html` — page composition baseline
- `forgesearch-preview.html` — command-bar spine baseline

## Rule

All production UI updates in `apps/web` or `app/` must reference this baseline and avoid visual drift.
